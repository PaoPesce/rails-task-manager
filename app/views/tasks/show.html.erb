<h1><%= @task.title %></h1>

<h2><%= @task.details %></h2>

<p>Click to complete task</p>

<%= link_to 'back to list', tasks_path %>

<%= link_to 'edit', edit_task_path %>

<%= link_to 'borrar', task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
 %>
